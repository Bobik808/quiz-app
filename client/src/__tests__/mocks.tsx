import React from 'react';
import Deck from '../components/deck.component'
import {getDeckFromName, deleteCard, DeckTypes} from '../types/types'

export const deck1 = <Deck getDeckFromName={getDecksFromName('cunt'): DeckType} deleteCard={} updateDecks={} refresh={} />;